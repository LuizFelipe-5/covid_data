import 'package:flutter/material.dart';

class CustomAppBarContinent extends StatelessWidget {
  const CustomAppBarContinent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
